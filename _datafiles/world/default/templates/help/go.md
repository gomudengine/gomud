# Help for ~go~

The ~go~ command is used to travel from area to area.

Typing ~go~ is unnecessary though, you can just type the exit name.

## Usage:

  ~go north~  
  This is identical to typing ~north~ by itself to exit the room.