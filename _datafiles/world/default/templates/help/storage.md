# Help for ~storage~

The ~storage~ command lets you store and unstore items.  
These items can be retrieved at various locations in the world.

## Usage:

  ~storage~ - See what you have in storage

  ~unstore [item]~ - Remove an item from storage
  
  ~store [item]~ - Add an item in storage