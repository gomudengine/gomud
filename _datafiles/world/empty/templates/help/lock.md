# Help for ~lock~

The ~lock~ command attempts to re-lock any exit or container.  
You'll need an appropriate *key* in your backpack or on your keyring.

## Usage:

  ~lock [exit name/container name]~ - This locks it if it is unlocked.

**See also:** ~help unlock~, ~help keyring~, ~help picklock~